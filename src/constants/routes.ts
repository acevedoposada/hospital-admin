export enum Routes {
  HOME = 'inicio',
  MEDICAMENTS = 'medicamentos',
  APPOINTMENTS = 'citas',
  SCHEDULED_APPOINTMENTS = 'programadas',
  SCHEDULE_APPOINTMENT = 'programar',
  EXAMS = 'examenes',
  CLINIC_HISTORY = 'historia-clinica',
}

export enum MainRoutes {
  HOME = Routes.HOME,
  MEDICAMENTS = Routes.MEDICAMENTS,
  APPOINTMENTS = Routes.APPOINTMENTS,
  EXAMS = Routes.EXAMS,
  CLINIC_HISTORY = Routes.CLINIC_HISTORY,
}
